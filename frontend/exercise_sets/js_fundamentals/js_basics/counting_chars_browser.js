// Prompts a user for a phrase and outputs its length.

const phrase = prompt('Please enter a phrase: ');
const length = phrase.length;

console.log(`There are ${length} characters in "${phrase}".`);
